apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
  namespace: my-namespace
data:
  provider.yaml: |
    apiVersion: 1
    providers:
    - name: default
      type: file
      options:
        path: /var/lib/grafana/dashboards