apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics-service
  namespace: my-namespace
spec:
  type: ClusterIP
  selector:
    app: kube-state-metrics
  ports:
  - port: 8080
    targetPort: 8080
